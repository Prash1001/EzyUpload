rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /uploads/{allPaths=**} {
      allow read: if true; // Allow read access to all files in the 'uploads' directory
      allow write: if request.auth != null; // Allow write access to authenticated users
    }
  }
}
